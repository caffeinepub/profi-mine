{
  "frontend/src/App.tsx": "Main application component with TanStack Router configuration including routes for login, dashboard, payment success, and payment failure pages.",
  "frontend/src/components/layout/RootComponent.tsx": "Root route component that handles authentication state, profile setup modal display, and renders the app layout.",
  "frontend/src/pages/DashboardPage.tsx": "Main dashboard page with tab navigation for inputs, projections, sensitivity analysis, export, and usage guide, wrapped in project and scenario providers, redirecting unauthenticated users to login using TanStack Router navigation.",
  "frontend/src/utils/calculations.ts": "Financial calculation engine that computes production profiles, cash flows, NPV, IRR, ROI, and other mining project metrics.",
  "frontend/src/utils/formatters.ts": "Utility functions for formatting currency, percentages, numbers, and dates for display throughout the application.",
  "frontend/src/constants/glossary.ts": "Dictionary of mining and financial terminology with definitions used in tooltips and the usage guide.",
  "frontend/src/constants/guideContent.ts": "Structured guide content for the Usage Guide tab; the PDF Export section has been removed from the Export Options section (only CSV Export and Chart Images remain), and the Getting Started step 6 now references CSV instead of PDF.",
  "frontend/src/index.css": "Global styles with mining-themed OKLCH color palette; updated print media queries to override ScrollArea overflow clipping (so all table rows print), fix chart container dimensions for Recharts SVGs, hide export buttons during print, and convert the grid layout to block for single-column PDF output.",
  "frontend/tailwind.config.js": "Tailwind CSS configuration with custom mining theme colors, typography, and animation settings.",
  "frontend/src/contexts/ProjectContext.tsx": "Project context provider managing mining project inputs, financial calculations, and subscription tier info; now also exposes romUsageCount (derived from userProfile) and an incrementRomUsage async helper that calls the backend and invalidates the profile cache.",
  "frontend/src/hooks/useQueries.ts": "Custom React Query hooks for all backend operations including user profile management, project CRUD, Stripe configuration, export limit checking, ROM usage tracking, and a new `useMarkUserAsPremium` mutation that calls `actor.markUserAsPremium()` and invalidates the `currentUserProfile` cache on success.",
  "frontend/src/hooks/useExcelExport.ts": "Hook for exporting financial projections to CSV format that checks export limits via backend canExport(), decrements export count, and generates comprehensive data including summary, assumptions, production, income statement, and cash flow.",
  "frontend/src/components/export/ExportTab.tsx": "Export tab component providing only CSV export with Free tier export limit tracking (2 total exports), displaying remaining exports counter and upgrade prompts when limits are reached, plus information about individual chart export buttons. PDF export option has been removed.",
  "frontend/src/pages/LoginPage.tsx": "Login page component that displays branding, feature list, and login button, redirecting authenticated users to the dashboard using TanStack Router navigation. The feature list now references \"Professional CSV exports\" instead of \"Professional Excel & PDF exports\".",
  "frontend/src/components/subscription/SubscriptionModal.tsx": "Subscription modal showing Free and Premium tier options; the Upgrade button calls `useCreateCheckoutSession` (no items needed — backend handles Premium tier internally), shows a loading spinner with \"Processing…\" text while pending, and displays an error toast on failure.",
  "frontend/src/components/project/SaveProjectModal.tsx": "Save project modal that validates user's subscription tier usage limits before allowing saves, displays current usage count, and shows upgrade prompt when monthly model limit is reached.",
  "frontend/src/components/dashboard/DashboardHeader.tsx": "Dashboard header with branding, project name, and a \"Models & Exports\" usage indicator that shows ROM Tonnage edits used (e.g., \"2/3 ROM Tonnage Edits\") and exports remaining for Free tier users, or \"Unlimited ROM Tonnage\" for Premium users; passes romUsageCount and romLimit to SubscriptionModal.",
  "frontend/src/components/auth/ProfileSetupModal.tsx": "Profile setup modal for new users collecting name, email, and organization with tier selection between Free and Premium; now includes romUsageCount: BigInt(0) in the new profile object to satisfy the updated UserProfile type.",
  "frontend/src/config/stripe.ts": "Stripe configuration file containing the publishable key constant for frontend payment initialization.",
  "frontend/src/hooks/useCreateCheckoutSession.ts": "React Query mutation hook that calls `actor.createPremiumCheckoutSession()`, parses the JSON response to extract the Stripe Checkout session URL, validates it is non-empty, and redirects the user via `window.location.href` on success.",
  "frontend/src/components/payment/PaymentSuccess.tsx": "Payment success page that calls `markUserAsPremium` once on mount via a ref guard; shows a loading spinner while pending, a success confirmation with Premium feature list on success, and an error state with a Retry button and support instructions on failure.",
  "frontend/src/components/payment/PaymentFailure.tsx": "Payment failure page component that displays an error message explaining possible reasons for payment failure and provides options to retry or contact support.",
  "frontend/src/hooks/usePdfExport.ts": "PDF export hook that checks export limits, decrements the count, then waits for two animation frames plus an 800ms timeout to ensure all Recharts SVGs and table content are fully rendered before calling window.print().",
  "frontend/src/hooks/useChartExport.ts": "Chart export hook that fixes SVG selection by picking the largest SVG element in the container (the chart, not the download icon), clones it with explicit dimensions and inlined styles, then renders it to a 2x-resolution canvas and triggers a PNG download.",
  "frontend/src/components/projections/ProjectionsTab.tsx": "Projections tab component that displays financial tables and charts with a unique identifier (id=\"projections-content\") to enable targeted PDF printing of only this tab's content.",
  "frontend/src/components/charts/CumulativeCashFlowChart.tsx": "Cumulative cash flow line chart with a separate `chartOnlyRef` attached to a dedicated wrapper div containing only the ResponsiveContainer, so the export hook captures the chart SVG and not the download button icon.",
  "frontend/src/components/charts/ProductionBarChart.tsx": "Annual production bar chart with a separate `chartOnlyRef` attached to a dedicated wrapper div containing only the ResponsiveContainer, so the export hook captures the chart SVG and not the download button icon.",
  "frontend/src/components/charts/CostBreakdownPieChart.tsx": "Cost breakdown pie chart with a separate `chartOnlyRef` attached to a dedicated wrapper div containing only the ResponsiveContainer, so the export hook captures the chart SVG and not the download button icon.",
  "frontend/src/components/inputs/CostStructureSection.tsx": "Cost structure input section with fields for CAPEX, operating costs, royalties, and a flexible closure year selector; the duplicate Equity Ratio and Interest Rate fields have been removed (they belong exclusively in FinancingSection).",
  "frontend/package.json": "Frontend package configuration with all dependencies including newly added jspdf, jspdf-autotable, and html2canvas libraries for PDF and image export functionality.",
  "frontend/src/types/external.d.ts": "TypeScript declaration file providing type definitions for external libraries html2canvas, jspdf, and jspdf-autotable to resolve module import errors.",
  "frontend/src/components/projections/IncomeStatementTable.tsx": "Income statement table wrapped in a `print-table-container` div with `print-scroll-area` class on the ScrollArea, enabling print CSS to override overflow/height constraints so all rows are visible in PDF exports.",
  "frontend/src/components/projections/CashFlowStatementTable.tsx": "Cash flow statement table wrapped in a `print-table-container` div with `print-scroll-area` class on the ScrollArea, enabling print CSS to override overflow/height constraints so all rows are visible in PDF exports.",
  "frontend/src/components/inputs/ReservesProductionSection.tsx": "Reserves and production input section that enforces the Free Tier 3-use ROM limit: tracks romUsageCount from context, increments it on first ROM change per session, disables all ROM inputs and shows an upgrade alert with a SubscriptionModal trigger when the limit is reached, and shows remaining edits for Free tier users below the limit.",
  "frontend/src/components/inputs/DynamicArrayInput.tsx": "Dynamic array input component for year-by-year values with add/remove year buttons; now accepts an optional disabled prop that makes all inputs read-only and disables all buttons when set."
}